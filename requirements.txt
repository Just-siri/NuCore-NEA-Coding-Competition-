# Core dependencies
anthropic>=0.40.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
pandas>=2.0.0

# PDF extraction (required for File 5 PDF input)
pdfplumber>=0.10.0

# Word document extraction (required for .docx input)
python-docx>=1.1.0